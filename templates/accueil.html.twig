<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Accueil {% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>

    {% if app.user %}
        <div class="mb-3">
            Vous êtes connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}

		<a href="{{ path('app_logout') }}">Logout</a>

    	<h1>Bienvenue</h1>
    	<a href="{{ path('listUsers') }}">Gestion des utilisateurs</a>

        <br><br>
    	<a href="{{ path('listRoles') }}">Gestion des rôles</a>  

        <br><br>
        <a href="{{ path('listDiaporamas') }}">Gestion des diaporamas</a>

        <br><br>
        <a href="{{ path('listEntites') }}">Gestion des entités (organismes, entreprises)</a>                    	

    </body>
</html>
